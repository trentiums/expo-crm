import { styled } from "./utils/styled";
import { GestureHandlerRootView } from "react-native-gesture-handler";

export const GestureHandlerRootContainer = styled(GestureHandlerRootView)`
  flex: 1;
`;
